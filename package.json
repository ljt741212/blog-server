{
  "name": "blog-server",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter server start:dev",
    "build": "pnpm --filter server build",
    "start:prod": "pnpm --filter server start:prod",
    "lint": "pnpm --filter server lint",
    "test": "pnpm --filter server test",
    "test:e2e": "pnpm --filter server test:e2e"
  },
  "dependencies": {
    "@blog/ai-agent": "workspace:*"
  }
}